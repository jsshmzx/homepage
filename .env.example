# Umami Analytics Configuration
# Set these environment variables to enable Umami analytics tracking

# Your Umami website ID (required for tracking)
# Example: NEXT_PUBLIC_UMAMI_WEBSITE_ID=a1b2c3d4-e5f6-7890-abcd-ef1234567890
NEXT_PUBLIC_UMAMI_WEBSITE_ID=

# URL to the Umami tracking script (e.g., https://analytics.yourdomain.com/script.js)
# Example: NEXT_PUBLIC_UMAMI_SRC=https://analytics.yourdomain.com/script.js
NEXT_PUBLIC_UMAMI_SRC=

# Tracked Events:
# - game_start: When a new game is started (includes difficulty, gridSize, imageIndex)
# - game_complete: When puzzle is completed (includes difficulty, gridSize, moves, durationSeconds, imageIndex)
# - difficulty_change: When difficulty level is changed (includes from, to, gridSize)
# - image_select: When a puzzle image is selected (includes imageIndex, previousImage)
# - piece_move_milestone: Every 5 moves (includes moves, difficulty, imageIndex)
# - play_again: When user clicks play again after completing (includes previousMoves, difficulty)
